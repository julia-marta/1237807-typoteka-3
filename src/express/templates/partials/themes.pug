mixin themes(categories)
  ul.themes(class!=attributes.class || ``)
    each category in categories
      li.themes__item
        a.themes__item-link(href="#")
          | #{category.name} 
          sup #{category.count}

        if attributes.class
          button.themes__remove-btn(type="button") Удалить категорию